# Tesla TV Pro

专为 Tesla 车载屏幕优化的私人影院，支持行车模式播放。

## 功能特性

- 🎬 **Canvas 渲染** - 绕过 Tesla 视频检测限制
- 📱 **Netflix 风格 UI** - 现代化设计，触控友好
- 🔊 **完整控制** - 播放/暂停、进度、音量、全屏
- ⌨️ **键盘快捷键** - 空格播放、方向键跳转、F 全屏

## 部署到 Cloudflare Pages

### 方法一：拖拽上传
1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 **Workers & Pages** > **Create application** > **Pages**
3. 选择 **Upload assets**
4. 拖拽整个 `tesla-tv` 文件夹上传
6. 点击 **Deploy site**

> **⚠️ 重要提示**：
> 由于 Cloudflare Pages 强制使用 HTTPS，请确保你的 `js/api.js` 中配置的影视 API 源也支持 `https://` 协议。如果 API 仅支持 HTTP，浏览器会因为 "Mixed Content" 安全策略而拦截请求，导致首页无法加载数据。

### 方法二：CLI 部署
```bash
npm install -g wrangler
wrangler pages deploy . --project-name=your-tesla-tv
```

## 本地测试

```bash
npx serve -p 3000
# 访问 http://localhost:3000
```

## 文件结构

```
tesla-tv/
├── index.html      # 主页（Netflix 风格）
├── player.html     # 播放器页面（Canvas 渲染）
├── _headers        # Cloudflare 缓存配置
└── _redirects      # 路由重定向
```

## 使用方法

1. 在 Tesla 浏览器中访问部署后的 URL
2. 选择视频或输入 m3u8 地址
3. 点击播放即可

## 技术原理

Tesla 浏览器会检测并限制 `<video>` 标签的行车播放。本项目使用 **HLS.js 解码 + Canvas 渲染** 绕过检测：

1. 隐藏的 `<video>` 元素用于解码
2. Canvas 绘制视频帧，Tesla 无法检测
3. 自定义控制栏覆盖在 Canvas 上

## License

仅供个人学习使用
